Prerequisites
Before running the bot, ensure you have the following installed:
• Python 3.8 or higher
• pip (Python package installer)
Required Libraries
The bot relies on several Python libraries. Below is the list of required libraries:
• python-telegram-bot==20.3
•SQLAlchemy==2.0.17
• APScheduler==3.10.4
These libraries can be installed using pip
Creating a Virtual Environment
Open your terminal or command prompt in the project directory and run the following command:
python -m venv venv
This command creates a new virtual environment named venv in your project directory.
Activating the Virtual Environment
[,
• On Windows:
venv\Scripts \activate



